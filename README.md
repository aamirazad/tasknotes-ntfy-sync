# TaskNotes to ntfy

A single-container service that continuously pulls an Obsidian Sync vault and sends due
TaskNotes reminders through ntfy. See `build-docs/application-build-plan.md` for the design;
the operator runbook is completed with the container implementation.

